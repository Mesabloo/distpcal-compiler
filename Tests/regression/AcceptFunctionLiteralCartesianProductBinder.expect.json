{
  "status": "xfail",
  "reason": "types now that `\\X` is in builtinContext, but has no Go compilation — a product's elements are pairs, and a tuple is an anonymous struct only its own site can name"
}
